Constants that are included in the [[Instruction]] are called **immediate values**. 

The processor can use immediate values **instantly** during the computations without cycles for fetching the actual value from storage. 