机器指令的操作码 (OP) 需要映射到控制存储器 (CM) 的入口地址。 #重点

1. **一级功能转换（拼接法）**: 
    - 将操作码 $\theta$ 与固定位进行 **拼接**。
    - 示例：$\text{入口地址} = \text{OP} \times 4$（相当于后补两位 `00`）。
2. **二级功能转换**: 
    - 先按指令类型分类，再进行一级转换。
3. **PLA 电路**: 
    - 使用逻辑阵列将不规则的 OP 码翻译为微地址。

