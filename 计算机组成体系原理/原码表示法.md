原码（Sign-Magnitude）是最简单的机器数表示法。

## 定义

- **符号位**: 最高位表示符号（0 为正，1 为负）。
- **数值位**: 剩余位表示数值的绝对值。

## 特点

- **优点**: 简单直观，与真值的转换非常容易。
- **缺点**: 
    - 加减法运算复杂（需要判断符号并决定是加还是减）。
    - **零的表示不唯一**: 存在 $+0$（如 `0000`）和 $-0$（如 `1000`）。 #重点
