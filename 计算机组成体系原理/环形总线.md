环形总线是一种用于连接 [[处理器内核]]、缓存和 IO 接口的总线拓扑结构。

- **应用实例**：在高性能服务器芯片内部，常采用环形总线连接大量内核以及 DDR4、SAS、PCIe 等接口。
- **特点**：
    - **高效通信**：允许数据在环路中快速传输。
    - **高集成度**：是实现 [[SOC]] 内部各组件互联的关键技术。
- **功能**：负责内核与内核之间、内核与 IO 设备之间的数据交换。