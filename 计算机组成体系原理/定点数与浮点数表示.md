在计算机中，根据小数点位置是否固定，数值表示分为 [[定点数表示]] 和 [[浮点数表示]]。

## 定点数与浮点数的对比

- **数值范围**: 在位数相同的情况下，浮点数能表示的范围远大于定点数。
- **精度**: 定点数在整个范围内精度一致；浮点数在不同阶码下精度不同（阶码越大，绝对精度越低）。
- **硬件实现**: 定点运算器结构简单；浮点运算器需要处理对阶、规格化等操作，逻辑复杂。 #重点
- **溢出处理**: 定点数容易发生溢出；浮点数只有在阶码超出范围时才溢出。
