#import "@preview/ctheorems:1.1.3": *

#set page(paper: "a4", margin: (top: 1.5cm, bottom: 1.5cm, left: 1.8cm, right: 1.8cm))
#set text(font: ("Noto Serif CJK SC", "Times New Roman"), size: 11pt)
#set par(first-line-indent: 2em, leading: 0.65em)

#let 科目 = "操作系统与分布式计算"
#let 总分 = 100
#let blank(len: 4em) = underline(h(len))

#align(center)[
  #block(text(weight: "bold", size: 22pt)[#科目]) \
  #block(text(weight: "bold", size: 16pt)[期末考试试题]) \
  #text(size: 10pt, fill: gray)[
    考试时间：120分钟 | 满分：#总分 分 \
    适用班级：BIT计算机学院 | 任课教师：宋红
  ]
]

#line(length: 100%)

#move(dy: 0.3em)[
  
  #grid(columns: (1fr, 1fr, 1fr, 1fr), [
    姓名：#blank(len: 5em)
  ], [
    学号：#blank(len: 5em)
  ], [
    班级：#blank(len: 5em)
  ], [
    得分：#blank(len: 5em)
  ])
]

#line(length: 100%)

#align(center)[
  #text(weight: "bold", size: 13pt)[注意事项]
]
\ 1. 所有答案一律写在答题纸上，写在试卷上无效。\
2. 交卷时请将试卷和答题纸一并上交。\
3. 可以使用非编程计算器。

#align(center, text(weight: "bold", size: 14pt)[一、单项选择题（每题 2 分，共 30 分）])

#set enum(numbering: "1)", spacing: 0.5em)

+ 操作系统的基本类型是（ ）。
   
  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 批处理系统、分时系统及实时系统
  ], [
    B. 单用户系统、多用户系统及网络系统
  ], [
    C. 单用户系统、多用户系统及实时系统
  ], [
    D. 批处理系统、分时系统及多用户系统
  ])

+ 在操作系统中，用户程序请求操作系统服务的唯一方式是（ ）。
  
  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 系统调用
  ], [
    B. 中断
  ], [
    C. 异常
  ], [
    D. 函数调用
  ])

+ 下列哪个调度算法可能导致 Belady 异常？（ ）
  
  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
      A. LRU
    ], [
      B. OPT
    ], [
      C. FIFO
    ], [
      D. Clock
    ])

+ 在进程的三状态模型中，下列哪种转换是不可能发生的？（ ）

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 就绪态 → 运行态
  ], [
    B. 运行态 → 就绪态
  ], [
    C. 阻塞态 → 运行态
  ], [
    D. 阻塞态 → 就绪态
  ])

+ 采用段式存储管理时，逻辑地址到物理地址的转换是通过（ ）实现的。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. MMU 进行位拼接
  ], [
    B. 段表进行加法拼接
  ], [
    C. 页表进行位拼接
  ], [
    D. 直接地址映射
  ])

+ 在生产者-消费者问题中，若将 P 操作的顺序颠倒为先 P(mutex) 再 P(empty)，则当缓冲区满时会导致（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 生产者饥饿
  ], [
    B. 死锁
  ], [
    C. 消费者饥饿
  ], [
    D. 正常运行
  ])

+ 银行家算法用于（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 死锁预防
  ], [
    B. 死锁避免
  ], [
    C. 死锁检测
  ], [
    D. 死锁解除
  ])

+ 以下哪种 I/O 控制方式在传输过程中不需要 CPU 干预数据搬运？（ ）

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 程序直接控制方式
  ], [
    B. 中断驱动方式
  ], [
    C. DMA 方式
  ], [
    D. 三种方式都需要
  ])

+ 在 UNIX 混合索引结构中，假设块大小为 1KB，块指针大小为 4B，则二级间接块可寻址的最大文件大小约为（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 256KB
  ], [
    B. 64MB
  ], [
    C. 16GB
  ], [
    D. 4TB
  ])

+ 下列哪种磁盘调度算法可能导致"磁壁粘着"（饥饿）现象？（ ）

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. FCFS
  ], [
    B. SSTF
  ], [
    C. SCAN
  ], [
    D. C-SCAN
  ])

+ 在虚拟页式存储系统中，缺页中断处理时，若淘汰的页面已被修改，则需（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 直接覆盖
  ], [
    B. 回写到外存
  ], [
    C. 清零后释放
  ], [
    D. 仅将合法位清零
  ])

+ Linux 伙伴系统（Buddy System）中，内存块的合并条件是两个块（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 大小相同且物理地址相邻
  ], [
    B. 大小相同且逻辑地址相邻
  ], [
    C. 大小相同且属于同一进程
  ], [
    D. 大小相同且页帧编号相同
  ])

+ 文件系统中，硬链接与软链接的主要区别在于（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 硬链接不能跨文件系统，软链接可以
  ], [
    B. 软链接不能跨文件系统，硬链接可以
  ], [
    C. 硬链接只能链接目录
  ], [
    D. 二者没有区别
  ])

+ 以下关于管程（Monitor）的描述，错误的是（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 管程自动保证互斥
  ], [
    B. 管程使用条件变量实现同步
  ], [
    C. 同一时间可有多个进程进入管程
  ], [
    D. 管程将共享数据和对数据的操作封装在一起
  ])

+ SPOOLing 技术可以实现（ ）。

  #grid(columns: (1fr, 1fr), gutter: 0.3em, [
    A. 虚拟内存
  ], [
    B. 虚拟设备
  ], [
    C. 虚拟文件系统
  ], [
    D. 虚拟处理器
  ])

#align(center, text(weight: "bold", size: 14pt)[二、填空题（每空 2 分，共 20 分）])

+ 操作系统最基本的四个管理功能是处理机管理、存储管理、#blank() 和 #blank()。

+ 进程的三种基本状态是 #blank()、#blank() 和阻塞态。

+ 死锁产生的四个必要条件是互斥、#blank()、不可剥夺和 #blank()。

+ 虚拟存储器实现的理论基础是 #blank() 原理。

+ TLB（快表）利用程序的 #blank() 性来提高地址转换效率。

+ 在 I/O 软件层次结构中，最接近硬件的层次是 #blank()。

+ UNIX 系统中，文件控制块（FCB）被称为 #blank()。


#align(center, text(weight: "bold", size: 14pt)[三、简答题（每题 5 分，共 15 分）])

+ 简述操作系统进入临界区的四个准则。

+ 简述分页存储管理与分段存储管理的主要区别。

+ 简述写时拷贝（Copy-on-Write）技术的基本原理及其优点。


#align(center, text(weight: "bold", size: 14pt)[四、计算与分析题（共 35 分）])

#set enum(numbering: "1.", spacing: 0.5em)

+ 本题 8 分

  某系统采用银行家算法避免死锁，当前系统资源分配情况如下（资源类型 A、B、C 的总量分别为 10、5、7）：

  #table(
    columns: (auto, auto, auto, auto, auto, auto, auto, auto, auto, auto),
    inset: 5pt,
    align: center,
    stroke: 0.5pt,
    [进程], [Allocation], [#move(dy: -0.2em)[]], [#move(dy: -0.2em)[]], [Max], [#move(dy: -0.2em)[]], [#move(dy: -0.2em)[]], [Need], [#move(dy: -0.2em)[]], [#move(dy: -0.2em)[]],
    [#move(dy: -0.2em)[]], [A], [B], [C], [A], [B], [C], [A], [B], [C],
    [P0], [0], [1], [0], [7], [5], [3], [], [], [],
    [P1], [2], [0], [0], [3], [2], [2], [], [], [],
    [P2], [3], [0], [2], [9], [0], [2], [], [], [],
    [P3], [2], [1], [1], [2], [2], [2], [], [], [],
    [P4], [0], [0], [2], [4], [3], [3], [], [], [],
  )

  (1) 请计算 Available 向量和每个进程的 Need 矩阵，填入表中。（2 分）\
  (2) 当前系统是否处于安全状态？若安全，给出一个安全序列。（4 分）\
  (3) 若进程 P1 请求资源 (1, 0, 2)，系统能否分配？请说明理由。（2 分）

+ 本题 9 分

  假设系统采用请求分页式虚拟存储管理，页大小为 4KB。一个作业的页面访问序列为：
  \[
  \mathbf{7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2, 1, 2, 0, 1, 7, 0, 1}
  \]
  系统分配给该作业的物理页帧数为 3 个（固定驻留集）。

  (1) 分别采用 FIFO 和 LRU 置换算法，计算各自的缺页次数和缺页率。（6 分）\
  (2) 什么是 Belady 异常？FIFO 算法是否可能发生 Belady 异常？LRU 呢？为什么？（3 分）

+ 本题 6 分

  假设磁盘请求的柱面序列为：98, 183, 37, 122, 14, 124, 65, 67。当前磁头位于 53 号柱面，正在向柱面号增加的方向移动。

  分别采用 SCAN（电梯算法）和 C-SCAN（循环扫描算法）调度算法，计算磁头移动的柱面数。要求写出寻道过程。

+ 本题 6 分

  某系统采用二级页表管理，逻辑地址空间大小为 64MB，页大小为 1KB，页表项大小为 2B。请计算：

  (1) 逻辑地址中页目录号的位数和页表索引的位数。（2 分）\
  (2) 页目录表的大小（占多少页）。（2 分）\
  (3) 若将页大小改为 4KB，页目录号的位数变为多少？（2 分）

+ 本题 6 分

  用信号量实现读者-写者问题（读者优先）。请补充以下代码中的空缺部分。

  ```c
  semaphore mutex = 1;  // 保护 readcount
  semaphore wrt = 1;    // 写者互斥信号量
  int readcount = 0;    // 当前读者数量

  // 读者进程
  void reader() {
    while (true) {
      _____(1)_____;
      readcount++;
      if (_____(2)_____) _____(3)_____;
      _____(4)_____;
      // 执行读操作
      _____(5)_____;
      readcount--;
      if (readcount == 0) _____(6)_____;
      _____(7)_____;
    }
  }

  // 写者进程
  void writer() {
    while (true) {
      _____(8)_____;
      // 执行写操作
      _____(9)_____;
    }
  }
  ```

#pagebreak()

#align(center, text(weight: "bold", size: 14pt)[试题参考答案])

#align(center, text(weight: "bold", size: 12pt)[一、单项选择题（每题 2 分，共 30 分）])


  #grid(columns: (1fr, 1fr, 1fr, 1fr, 1fr), gutter: 0.3em,
  `1) A`, `2) A`, `3) C`, `4) C`, `5) B`,
  `6) B`, `7) B`, `8) C`, `9) B`, `10) B`,
  `11) B`, `12) A`, `13) A`, `14) C`, `15) B`,
)

#align(center, text(weight: "bold", size: 12pt)[二、填空题（每空 2 分，共 20 分）])

1. 文件管理，设备管理 \
2. 就绪态，运行态 \
3. 请求与保持，循环等待 \
4. 局部性 \
5. 局部性 \
6. 中断处理程序 \
7. inode（索引节点）

#align(center, text(weight: "bold", size: 12pt)[三、简答题（每题 5 分，共 15 分）])

1． 进入临界区的四个准则：\
   (1) *互斥使用*：不能同时有两个进程在临界区内执行。\
   (2) *有空让进*：临界区外运行的进程不可阻止其他进程进入临界区。\
   (3) *有限等待*：不应使要进入临界区的进程无限期等待。\
   (4) *让权等待*：等待进入临界区的进程应释放处理机后阻塞等待。

2． 分页与分段的主要区别：\
   (1) *目的不同*：分页是为实现离散分配以消除外部碎片；分段是为满足用户模块化编程需求。\
   (2) *大小*：页大小固定（2 的幂次），段大小不固定由用户决定。\
   (3) *地址空间*：分页是线性地址空间的一维结构；分段是二维地址空间（段号 + 段内地址）。\
   (4) *地址转换*：页式采用位拼接；段式采用加法拼接。\
   (5) *共享保护*：分段更自然地支持按逻辑单元的共享与保护。

3． 写时拷贝（COW）技术：\
   *原理*：`fork()` 创建子进程时，不立即复制父进程的所有内存页，而是将页表项指向同一物理页帧并标记为只读。当任一进程试图写入共享页面时，触发缺页异常，内核分配新物理页帧，复制原内容后修改页表为可写。\
   *优点*：避免一次性拷贝所有数据，开销分散；只被写入的页面才真正复制，共享页面保留；提高进程创建效率。

#align(center, text(weight: "bold", size: 12pt)[四、计算与分析题（共 35 分）])

1． (1) Available = (3, 3, 2)，Need 矩阵如下：\
   P0: (7, 4, 3), P1: (1, 2, 2), P2: (6, 0, 0), P3: (0, 1, 1), P4: (4, 3, 1)\

   (2) 系统处于安全状态。安全序列：P1 → P3 → P4 → P0 → P2（或其他等价序列）。\
   检查过程：P1 的 Need(1,2,2) ≤ Available(3,3,2) → 分配后回收可用 (5,3,2)；\
   P3 的 Need(0,1,1) ≤ (5,3,2) → 可用 (7,4,3)；\
   P4 的 Need(4,3,1) ≤ (7,4,3) → 可用 (7,4,5)；\
   P0 的 Need(7,4,3) ≤ (7,4,5) → 可用 (7,5,5)；\
   P2 的 Need(6,0,0) ≤ (7,5,5) → 全部完成。\

   (3) 可以分配。P1 请求 (1,0,2) ≤ Need(1,2,2) 且 ≤ Available(3,3,2)，执行试探分配后：\
   Allocation(P1) = (3,0,2), Need(P1) = (0,2,2), Available = (2,3,0)。\
   此时存在安全序列 P1→P3→P4→P0→P2 或 P1→P3→P4→P2→P0，系统仍安全，故可分配。

2． (1) FIFO 置换算法：\
     7(缺) 0(缺) 1(缺) 2(换7) 0(中) 3(换0) 0(换1) 4(换2) 2(换3) 3(换0) 0(换4) 3(中) 2(换2) 1(换3) 2(换0) 0(换4) 1(换2) 7(换1) 0(换7) 1(换0)\
     缺页次数：15 次，缺页率：15/20 = 75%。\
     LRU 置换算法：\
     7(缺) 0(缺) 1(缺) 2(换7) 0(中) 3(换1) 0(中) 4(换2) 2(换3) 3(换0) 0(换4) 3(中) 2(换?) 1(换2) 2(中) 0(换1) 1(换2) 7(换0) 0(换7) 1(中)\
     缺页次数：12 次，缺页率：12/20 = 60%。

   (2) Belady 异常是指分配更多物理页帧时缺页次数反而增加的现象。FIFO 可能发生 Belady 异常（非栈式算法），LRU 不会发生（栈式算法，驻留集增大时缺页次数一定不增）。

3． 磁头从 53 开始向柱面号增加方向移动：\
   SCAN：53 → 65 → 67 → 98 → 122 → 124 → 183 → 37 → 14 \
   移动距离：(65-53)+(67-65)+(98-67)+(122-98)+(124-122)+(183-124)+(183-37)+(37-14) \
   = 12+2+31+24+2+59+146+23 = 299 个柱面。\
   C-SCAN：53 → 65 → 67 → 98 → 122 → 124 → 183 → 199(端头) → 0 → 14 → 37 \
   移动距离：(65-53)+(67-65)+(98-67)+(122-98)+(124-122)+(183-124)+(199-183)+(199-0)+(14-0)+(37-14) \
   = 12+2+31+24+2+59+16+199+14+23 = 382 个柱面。

4． (1) 页大小 1KB=2^10B，页内偏移 10 位。逻辑地址空间 64MB=2^26B。\
   页目录号位数 = 26 - 10 - 9 = 7 位（每页可存页表项数：1KB/2B=512=2^9，故页表索引 9 位）。\
   页目录号 7 位，页表索引 9 位。\
   (2) 页目录表项数：2^7 = 128 项，每项 2B，总大小 256B，占 1 页（1KB）。\
   (3) 页大小改为 4KB=2^12B，页内偏移 12 位。每页可存页表项数：4KB/2B=2048=2^11。\
   页目录号位数 = 26 - 12 - 11 = 3 位。

5． (1) P(mutex) \
   (2) readcount == 1 \
   (3) P(wrt) \
   (4) V(mutex) \
   (5) P(mutex) \
   (6) V(wrt) \
   (7) V(mutex) \
   (8) P(wrt) \
   (9) V(wrt)
