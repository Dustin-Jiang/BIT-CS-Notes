
## 方向性分类

- **单工通信**：只有一个方向的通信（如无线电广播）。
- **半双工通信**：双方都可以发送信息，但不能同时发送（如对讲机）。
- **全双工通信**：双方可以同时发送和接收信息（如电话）。

## 传输方式分类

- **串行传输**：比特流按顺序在一根传输线上逐位传输。成本低，适合远距离。
- **并行传输**：多个比特同时在多条传输线上传输。速度快，适合近距离（如计算机内部总线）。
