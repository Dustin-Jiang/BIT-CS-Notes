实现从 **IP 地址** 到 **MAC 地址** 的动态映射。

## 工作过程

1. **检查高速缓存**：查找自己的 ARP 缓存表是否有目标 IP 的 MAC。
2. **广播请求**：若无，发送广播 ARP 请求分组。
3. **单播响应**：目标主机收到请求后，回送单播 ARP 响应分组。
4. **更新缓存**：源主机将映射存入 ARP 表。

## 注意事项

- **生存时间 (TTL)**：ARP 表项通常 20 分钟过期。
- **作用范围**：ARP 仅在同一个局域网内有效。跨网通信需通过 ARP 找到默认网关路由器的 MAC 地址。
