多路访问控制 (MAC) 解决如何协调多个节点对单一共享信道的争用。

1. **静态划分信道**：
    - [[频分复用]]、[[时分复用]]、波分复用 (WDM)、[[码分复用]]。
2. **动态媒体接入控制 (多点接入)**：
    - **随机接入**：用户随机发送，可能冲突。如 [[ALOHA协议]], [[CSMA协议]]。
    - **受控接入**：每一时刻仅一个站点发送。如 [[轮询协议]] (Polling)、[[令牌环网]] 传递 (Token Passing)。
