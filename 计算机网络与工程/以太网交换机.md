以太网交换机 (Switch) 实质上是一个多端口的网桥。

## 特点 #重点

- **隔离碰撞域**：每个端口都是一个独立的碰撞域。
- **全双工**：支持多个端口对同时通信，互不干扰。
- **独占带宽**：$N$ 个用户共享 $10	\text{Mbps}$ 时，交换机总容量可达 $N 	\times 10	\text{Mbps}$。
- **交换方式**：
	- **存储转发 (Store-and-forward)**：完整接收后再转发，可靠性高。
	- **直通交换 (Cut-through)**：只查目的地址就立即转发，时延小。
