1. 发送方连续发送多个帧（受发送窗口 $W_S$ 限制）。
2. 接收方只按序接收帧。若收到乱序帧，则丢弃，并对已收到的按序最高帧发回确认。
3. 发送方若收到某帧的超时，或收到 NAK，则**必须重传该帧及其后的所有已发送帧**。

## 特点 #重点

- **接收窗口大小 $W_R = 1$**。
- 若采用 $n$ 位编号，则发送窗口限制为 $W_S \le 2^n - 1$。
- **缺点**：信道误码率高时，会导致大量不必要的重复传输。
