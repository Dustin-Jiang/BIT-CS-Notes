1. 发送方每发送一帧，就停止发送，等待接收方的确认帧。
2. 只有收到确认帧后，才发送下一帧。
3. 若规定时间内（超时）未收到确认，则重传当前帧。

## 特点

- **优点**：简单。
- **缺点**：信道利用率极低。 #重点
- **信道利用率** $U = \frac{T_f}{T_f + 2T_d}$（$T_f$ 为帧发送时延，$T_d$ 为单程传播时延）。

## 帧编号

- 为了区分重复帧，只需 1 bit 序号（0 和 1）即可。
