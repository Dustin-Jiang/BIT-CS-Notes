- 使用 **距离向量算法**。
- **距离 (Distance)**：又称跳数 (Hop Count)，每经过一个路由器跳数加 1。
- **限制**：最大距离为 15，16 表示不可达（适用于小型网络）。

### 三个要点

1. **和谁交换**：仅与**相邻**路由器交换。
2. **交换什么**：自己的**全部**路由表。
3. **何时交换**：按**固定时间间隔** (如 30s)。

### 优缺点

- **优点**：简单，实现开销小。
- **缺点**：网络规模有限；收敛慢，存在“坏消息传得慢”的问题（计数到无穷问题）。
